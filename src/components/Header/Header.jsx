import React, { useRef } from "react";
import styles from "./Header.css";


export default function Header() {
  return (
    <header className={styles.header}>
      Calendar - Notepad - Planer
    </header >
  )
}